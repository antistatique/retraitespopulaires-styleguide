<?php

/**
 * @file
 * Legacy Drupal 7 code to implements hooks.
 */

/**
 * Toolbar hook.
 *
 * Attache the custom icons.
 */
function rp_toolbar_toolbar_alter(&$items) {
  $items['administration']['#attached']['library'][] = 'rp_toolbar/icons';
}
