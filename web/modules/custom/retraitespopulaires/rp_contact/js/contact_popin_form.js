(function ($) {
  'use strict';

  // on domReady
  $(function () {
    var $popinForm = $('#rp-popin-form');

    // Form not found, skip everything
    if (!$popinForm.size()) {
      return;
    }

    // /////////////////////////////////////////////////////////////////////////////////////
    // vars

    // /////////////////////////////////////////////////////////////////////////////////////
    // binding

    // Check the Javascript Session Cookie doesn't exists before show the popin.

    // Show the popin after 6 seconds on the page.

    // Add Javascript Session Cookie after form submit ajax.

  });
})(jQuery);
