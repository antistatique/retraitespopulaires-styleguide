# Defines an event listner which can alters routes.
services:
  rp_offers.request:
    class: Drupal\rp_offers\Service\Request
    arguments:
      - '@entity_type.manager'
      - '@state'
      - '@plugin.manager.mail'
